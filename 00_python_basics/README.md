# 第一阶段：Python 基础语法

> 🎯 目标：掌握 Python 核心语法，为算法学习打下基础
> ⏱️ 预计时间：1-2 周

## 学习模块

### 01. 变量与数据类型 `./01_variables_types/`
- 基本类型：`int`, `float`, `str`, `bool`, `None`
- 类型转换
- 字符串操作

### 02. 控制流 `./02_control_flow/`
- 条件语句：`if`, `elif`, `else`
- 循环语句：`for`, `while`
- 控制关键字：`break`, `continue`, `pass`

### 03. 函数 `./03_functions/`
- 函数定义与调用
- 参数类型：位置参数、关键字参数、默认参数
- `lambda` 表达式
- 作用域与闭包

### 04. 集合类型 `./04_collections/`
- 列表 `list`：增删改查、切片、列表推导式
- 字典 `dict`：键值操作、字典推导式
- 集合 `set`：去重、交并差运算
- 元组 `tuple`：不可变序列

### 05. 面向对象基础 `./05_oop_basics/`
- 类与对象
- `__init__` 构造函数
- 继承与多态
- 特殊方法：`__str__`, `__repr__`

## 刷题必备技巧

```python
# 1. 列表推导式 - 快速构建列表
squares = [x**2 for x in range(10)]

# 2. enumerate - 同时获取索引和值
for i, val in enumerate(arr):
    print(f"索引 {i}: 值 {val}")

# 3. zip - 并行遍历多个列表
for a, b in zip(list1, list2):
    print(a, b)

# 4. sorted 与 key 参数
sorted(arr, key=lambda x: x[1], reverse=True)

# 5. 字典 get 方法 - 安全访问
count = d.get(key, 0)  # 不存在返回默认值 0

# 6. collections 模块
from collections import Counter, defaultdict, deque
counter = Counter(arr)  # 计数器
dd = defaultdict(list)  # 默认值字典
dq = deque()  # 双端队列
```

## 练习建议

1. 每学完一个模块，完成对应目录下的练习
2. 不要只看代码，动手敲一遍
3. 尝试用不同方法解决同一问题
